<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Sportzfy" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_dim</item>
        <item name="colorOnPrimary">@color/bg_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="Theme.Sportzfy.Splash" parent="Theme.Sportzfy">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Sportzfy.Player" parent="Theme.Sportzfy">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/player_bg</item>
        <item name="android:statusBarColor">@color/player_bg</item>
        <item name="android:navigationBarColor">@color/player_bg</item>
    </style>

    <style name="BottomSheetTheme" parent="Theme.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppBottomSheetDialogTheme</item>
        <item name="colorSurface">@color/surface</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="backgroundTint">@color/surface</item>
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>

    <style name="QualityDialogTheme" parent="Theme.MaterialComponents.Dialog">
        <item name="colorSurface">@color/surface</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="colorPrimary">@color/accent</item>
    </style>

</resources>
